version: "3.8"
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: proyect_web_III
      POSTGRES_USER: tu_usuario
      POSTGRES_PASSWORD: tu_contraseña
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
